// Lets now look at how to define and call functions
// Declare - Preforming a task
function greet(name) {
    console.log('Hello ' + String(name));
}

// Call
// greet('Morris');

// Types of Functions
// Calculate - have a return
function square(number) {
    return number * number;
}

console.log(square(2));